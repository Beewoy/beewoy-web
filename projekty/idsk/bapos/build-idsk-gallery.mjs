import { readFileSync, writeFileSync } from 'node:fs';
import { resolve } from 'node:path';

const root = resolve(new URL('.', import.meta.url).pathname);
const snapshot = JSON.parse(readFileSync(resolve(root, '../../../../beewoy-agents/references/idsk3/component-snapshot.json'), 'utf8'));
const escape = (value) => value.replaceAll('&', '&amp;').replaceAll('<', '&lt;').replaceAll('>', '&gt;');
const cards = snapshot.components.map((component) => `<article class="idsk-card"><div class="idsk-card__content"><h2 class="idsk-card__heading"><a class="govuk-link" href="${component.url}">${component.title}</a></h2><p class="idsk-card__description">${component.variants.length} zachytených sekcií/variantov, ${component.implementationExamples.length} implementačných ukážok.</p><details><summary>Všetky implementačné ukážky (${component.implementationExamples.length})</summary>${component.implementationExamples.map((example, index) => `<h3 class="govuk-heading-s">Ukážka ${index + 1}</h3><pre class="govuk-code"><code>${escape(example)}</code></pre>`).join('')}</details></div></article>`).join('\n');
const html = `<!doctype html><html lang="sk"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>IDSK 3 – kompletná knižnica komponentov</title><link rel="stylesheet" href="assets/frontend.min.css"></head><body class="govuk-frontend-supported"><a class="govuk-skip-link" href="#main-content">Preskočiť na hlavný obsah</a><main id="main-content" class="govuk-main-wrapper"><div class="govuk-width-container"><span class="govuk-caption-xl">komponenty.idsk3.gov.sk</span><h1 class="govuk-heading-xl">Všetky IDSK 3 komponenty a príklady</h1><p class="govuk-body-l">Automaticky vygenerované z oficiálneho katalógu. Každý komponent obsahuje všetky zachytené implementačné ukážky.</p><div class="govuk-grid-row">${cards}</div></div></main><script src="assets/frontend.file.js"></script></body></html>`;
writeFileSync(resolve(root, 'idsk-gallery.html'), html);
console.log(`Generated ${snapshot.components.length} components and ${snapshot.components.reduce((sum, component) => sum + component.implementationExamples.length, 0)} examples.`);
